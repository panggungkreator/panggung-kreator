import React from "react";
import SidebarLayoutClient from "./SidebarLayoutClient";

export const metadata = {
  title: "Sidebar Layout - Admin Panel",
};

export default function SidebarLayoutPage() {
  return <SidebarLayoutClient />;
}
